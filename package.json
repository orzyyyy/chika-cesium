{
  "name": "chika-cesium",
  "version": "0.0.0",
  "description": "A modal loader using cesium",
  "scripts": {
    "start": "nino go",
    "test": "nino test",
    "dist": "nino koei -c ./scripts/nino.koei.js",
    "compile": "nino compile",
    "dev": "nino koei -w-d",
    "lint": "eslint ./src/*.ts",
    "prettier": "node ./scripts/prettier.js",
    "lint-staged": "lint-staged",
    "backup": "npm config set registry=https://registry.npmjs.org && npm publish && npm config set registry=https://registry.npm.taobao.org",
    "pub": "npm run build && npm run compile && git push origin master && npm publish"
  },
  "keywords": [
    "cesium"
  ],
  "files": [
    "lib",
    "dist"
  ],
  "main": "lib/entry",
  "author": "zy9",
  "license": "MIT",
  "devDependencies": {
    "@types/classnames": "^2.2.7",
    "babel-eslint": "^10.0.1",
    "classnames": "^2.2.6",
    "dekko": "^0.2.1",
    "eslint": "^5.14.1",
    "husky": "^1.3.1",
    "lint-staged": "^8.1.4",
    "pre-commit": "^1.2.2",
    "prettier": "^1.15.3",
    "ts-loader": "^5.3.3"
  },
  "dependencies": {
    "cesium": "^1.54.0",
    "nino-cli": "^0.4.4"
  },
  "pre-commit": [
    "lint-staged"
  ],
  "lint-staged": {
    "*.{ts,js}": [
      "npm run prettier",
      "npm run lint",
      "git add"
    ]
  }
}
